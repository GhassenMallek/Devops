pipeline {
    agent any
    environment {
        NEXUS_VERSION = "nexus3"
        NEXUS_PROTOCOL = "http"
        NEXUS_URL = "172.10.0.140:8081"
        NEXUS_REPOSITORY = "maven-releases"
        NEXUS_CREDENTIAL_ID = "Nexus-Creds"
    }
    stages {
        
        stage("Maven Clean") {
            steps {
                script {
                    sh "mvn -f'Spring/tpAchatProject/pom.xml' clean -DskipTests=true"
                }
            }
        }
        stage("Maven Compile") {
            steps {
                script {
                    sh "mvn -f'Spring/tpAchatProject/pom.xml' compile -DskipTests=true"
                }
            }
        }
        stage("Maven Sonarqube") {
            steps {
                script {
                    sh "mvn -f'Spring/tpAchatProject/pom.xml' sonar:sonar -Dsonar.login=admin -Dsonar.password=Ghassen1998"
                }
            }
        }
        stage("Maven Build") {
            steps {
                script {
                    sh "mvn -f'Spring/tpAchatProject/pom.xml' package -DskipTests=true"
                }
            }
        }
    }
}
